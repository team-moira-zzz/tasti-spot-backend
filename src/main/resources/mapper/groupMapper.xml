<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tastispotbackend.domain.group.mapper.GroupMapper">
    <select id="selectInvitationCodeChk">
        SELECT COUNT(id) FROM SPOT_GROUP WHERE invitation_code = #{invitationCode}
    </select>

    <insert id="insertSpotGroup" parameterType="com.example.tastispotbackend.global.entity.SpotGroup">
        INSERT INTO SPOT_GROUP (
               id
             , owner_id
             , name
             , description
             , invitation_code
             , created_at
             , updated_at
        ) VALUES (
               #{id}
             , #{ownerId}
             , #{name}
             , #{description}
             , #{invitationCode}
             , #{createdAt}
             , #{updatedAt}
        )
    </insert>
</mapper>
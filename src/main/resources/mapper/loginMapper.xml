<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tastispotbackend.domain.login.mapper.LoginMapper">
    <select id="selectEmailChk">
        SELECT count(id) FROM USER WHERE email = #{email}
    </select>

    <select id="selectUserByEmail" resultType="com.example.tastispotbackend.global.entity.User">
        SELECT
               id
             , role
             , name
             , nickname
             , email
             , password
             , rtk
             , last_login_at AS lastLoginAt
             , created_at    AS createdAt
             , updated_at    AS updatedAt
        FROM USER
        WHERE email = #{email}
    </select>

    <insert id="insertUserLoginHistory" parameterType="com.example.tastispotbackend.global.entity.UserLoginHistory">
        INSERT INTO USER_LOGIN_HISTORY(
               user_id
             , success
             , ip_address
             , user_agent
             , failure_reason
             , login_time
        ) VALUES (
               #{userId}
             , #{success}
             , #{ipAddress}
             , #{userAgent}
             , #{failureReason}
             , #{loginTime}
        )
    </insert>

    <update id="updateUserWhenLogin">
        UPDATE USER
        SET
               rtk           = #{rtk}
             , last_login_at = CURRENT_TIMESTAMP
        WHERE id = #{userId}
    </update>
</mapper>